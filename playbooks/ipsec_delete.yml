---
- name: Delete IPsec Configuration
  hosts: ise_servers
  vars_files:
    - credentials.yml
  gather_facts: false
  tasks:
    - name: Delete IPsec configuration
      cisco.ise.ipsec_delete:
        ise_hostname: "{{ ise_hostname }}"
        ise_username: "{{ ise_username }}"
        ise_password: "{{ ise_password }}"
        ise_verify: "{{ ise_verify }}"
        hostName: "{{ target_hostname }}"
      register: result

    - name: Print Result
      ansible.builtin.debug:
        var: result
